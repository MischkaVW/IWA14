const firstName = 'John';
const age = 35;
hobby = 'Coding';

const logTwice = () => {
  console(parameter)
  console(parameter)
}

function hobby (hobby) {
  logTwice(`Hello, ${firstName} (${age}). I love ${hobby}!`)
}

hobby()